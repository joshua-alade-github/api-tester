{
  "buildCommand": "ng build --configuration production",
  "outputDirectory": "dist/api-tester/browser",
  "framework": "angular",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}